var config = {};

config.port = 3000;

//DataBase
config.database = '_workshop';
config.username = 'root';
config.password = 'root';
config.table_prefix = '';

//Pagination
config.paginate = true;
config.page_limit = 500;

//Secret
config.secret = 'etpourquoipas';

module.exports = config;